const printName = (name) => {
  console.log(name);
};

const print = () => {
  console.log("hello world");
};

const printMultiple = (name, age) => {
  console.log(name + " is " + age + " years old.");
};

const multiply = (number) => number * 2;

printName("Jason");
console.log(multiply(2));
